<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.dolbomi.mapper.UserMapper">

    <insert id="insert">
        INSERT INTO TBL_USER (USER_NAME, USER_GENDER, USER_BIRTH, USER_EMAIL,USER_PHONE_NUM,USER_ADDRESS,USER_PW)
        VALUES (#{userName}, #{userGender},#{userBirth},#{userEmail},#{userPhoneNum},#{userAddress},#{userPw})
    </insert>

    <select id="select" resultType="reviewVO">
        SELECT REVIEW_NUM, REVIEW_TITLE, REVIEW_CONTENT, REVIEW_WRITER, REVIEW_SCORE,REVIEW_DATE, REVIEW_UPDATE_DATE
        FROM TBL_REVIEW
        WHERE REVIEW_NUM = #{reviewNum}
    </select>

    <update id="update">
        UPDATE TBL_USER
        SET USER_NAME = #{userName}
        WHERE REVIEW_NUM = #{reviewNum}
    </update>

    <delete id="delete">
        DELETE FROM TBL_USER
        WHERE USER_EMAIL = #{userEmail}
    </delete>

</mapper>